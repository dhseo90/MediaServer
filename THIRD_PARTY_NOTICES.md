# Third-Party Notices

<!-- 이 파일은 ./server.sh write-dependency-notice 명령으로 생성합니다. -->

- schema: media-server.third-party-attribution.v2
- source inventory: config/third_party_attribution.json
- scope: binary bundle과 운영 배포 전에 확인해야 하는 third-party runtime/tool/model attribution

이 문서는 이 저장소의 Apache-2.0 라이선스가 third-party 구성요소를 재라이선스하지 않는다는 점을 명확히 하기 위한 배포 점검용 문서입니다.
실제 binary bundle에 포함되는 파일 목록은 배포 방식마다 달라질 수 있으므로, release 전에 포함 library/plugin/model/tool을 다시 확인해야 합니다.

| 구성요소 | 라이선스 | 버전 기준 | 감지 방법 | 용도 | 출처/배포 | 번들 기준 |
| --- | --- | --- | --- | --- | --- | --- |
| GStreamer, gst-rtsp-server, gst-plugins-base/good/bad | LGPL-family and plugin-specific upstream terms | 최소/기준: 미고정. CMake는 pkg-config에서 필요한 GStreamer 1.0 module 존재 여부를 확인합니다. 정책: GStreamer 1.0 API 계열을 사용하며, 실제 설치 버전은 pkg-config 결과를 release snapshot에 기록합니다. | gstreamer-1.0: pkg-config --modversion gstreamer-1.0; gstreamer-rtsp-server-1.0: pkg-config --modversion gstreamer-rtsp-server-1.0; gstreamer-pbutils-1.0: pkg-config --modversion gstreamer-pbutils-1.0; gstreamer-app-1.0: pkg-config --modversion gstreamer-app-1.0; gstreamer-webrtc-1.0: pkg-config --modversion gstreamer-webrtc-1.0; gstreamer-sdp-1.0: pkg-config --modversion gstreamer-sdp-1.0 | RTSP/RTP/WebRTC 파이프라인, 디코딩, 인코딩, appsrc/appsink, RTSP 서버 연동 | Homebrew, apt, dnf, pacman 같은 시스템 패키지 관리자 / 외부 runtime/development dependency이며 이 저장소에 vendoring하지 않음 | binary bundle에 GStreamer library/plugin을 포함하면 upstream license text와 plugin notice를 함께 포함합니다. |
| libnice and libnice GStreamer plugin | LGPL-family upstream terms | 최소/기준: 미고정. 설치 패키지와 GStreamer plugin 호환성을 우선합니다. 정책: GStreamer WebRTC ICE 기능에 필요한 libnice 설치 버전을 release snapshot에 기록합니다. | nice: pkg-config --modversion nice | GStreamer WebRTC의 ICE transport 지원 | 시스템 패키지 관리자 / 외부 runtime dependency이며 이 저장소에 vendoring하지 않음 | 번들에 포함하면 libnice license text와 upstream attribution을 유지합니다. |
| Cairo and Pango | LGPL/MPL-family upstream terms | 최소/기준: 미고정. 없으면 MEDIA_SERVER_USE_PANGOCAIRO=0으로 빌드합니다. 정책: PangoCairo가 발견되면 overlay text rendering에 사용하고, 설치 버전은 release snapshot에 기록합니다. | pangocairo: pkg-config --modversion pangocairo; cairo: pkg-config --modversion cairo; pango: pkg-config --modversion pango | 선택 overlay text rendering과 glyph 지원 | 시스템 패키지 관리자 / 외부 runtime/development dependency이며 이 저장소에 vendoring하지 않음 | 번들에 포함하면 Cairo, Pango, 전이 runtime library의 upstream license text를 함께 포함합니다. |
| ONNX Runtime | MIT | 최소/기준: 1.20.1 기본 설치 기준. 최신 시스템 패키지도 허용합니다. 정책: Linux 자동 설치 기본값은 1.20.1입니다. Homebrew/수동 설치 환경은 감지된 버전을 snapshot에 기록합니다. | libonnxruntime: pkg-config --modversion libonnxruntime | 선택 YOLO detector runtime과 실험적 Re-ID hook | Microsoft ONNX Runtime release archive 또는 시스템 패키지 관리자 / Linux에서 ./server.sh install 실행 시 third_party/onnxruntime로 다운로드될 수 있음 | 번들에 포함하면 ONNX Runtime MIT license와 다운로드한 release package 안의 notice 파일을 함께 포함합니다. |
| libsodium | ISC | 최소/기준: 미고정. 없으면 fallback password hashing 경로를 사용합니다. 정책: 발견되면 MEDIA_SERVER_USE_LIBSODIUM=1로 빌드하고, 설치 버전은 snapshot에 기록합니다. | libsodium: pkg-config --modversion libsodium | 환경에 있을 때 선택 password hashing과 crypto 지원 | 시스템 패키지 관리자 / 외부 optional dependency이며 이 저장소에 vendoring하지 않음 | 번들에 포함하면 libsodium ISC license text를 함께 포함합니다. |
| FFmpeg and ffprobe | LGPL/GPL depending on the installed build configuration | 최소/기준: 미고정. 검증 스크립트는 ffmpeg/ffprobe command 존재 여부와 실제 probe 성공 여부를 기준으로 판단합니다. 정책: 패키지 관리자 설치 버전을 사용합니다. release snapshot은 ffmpeg/ffprobe version과 GPL build flag 포함 여부를 기록합니다. | ffmpeg: ffmpeg -version; ffprobe: ffprobe -version | 검증 스크립트, sample probing, media diagnostics, 선택 helper workflow | 시스템 패키지 관리자 / 외부 command-line dependency이며 이 저장소에 vendoring하지 않음 | FFmpeg는 Apache-2.0 적용 대상으로 보지 않습니다. 번들 FFmpeg build가 GPL component를 켰는지 확인합니다. |
| Ultralytics YOLO model asset yolo11n.onnx | Provider-specific model terms; not relicensed by this repository | 최소/기준: 기본 모델 파일명 기준 yolo11n.onnx. URL은 MEDIA_SERVER_YOLO_MODEL_URL로 바꿀 수 있습니다. 정책: 기본 다운로드 URL은 Ultralytics assets v8.4.0의 yolo11n.onnx입니다. 실제 파일 hash는 snapshot에 기록합니다. | models/yolo11n.onnx: models/yolo11n.onnx | ./server.sh install이 다운로드하는 기본 선택 object detection model | Ultralytics GitHub release assets / 다운로드 asset이며 이 저장소에 commit하지 않음 | 재배포하거나 상업적으로 사용하기 전에 모델 제공자 license를 이 저장소 license와 별도로 확인합니다. |
| Optional YOLO layout verification model assets | Provider-specific model terms; not relicensed by this repository | 최소/기준: 선택 검증 전용이라 제품 실행 필수 조건이 아닙니다. 정책: 기본 검증 URL은 스크립트에 고정되어 있고 실제 파일 hash는 snapshot에 기록합니다. | models/yolov5n.onnx: models/yolov5n.onnx; models/yolov8n-640x640-end2end.onnx: models/yolov8n-640x640-end2end.onnx | verify-yolo-layouts 선택 검증에서 output layout/box/score 조합을 확인하는 추가 ONNX 모델 | Ultralytics YOLOv5 release asset과 별도 YOLOv8 end-to-end ONNX sample URL / 선택 검증 중 다운로드될 수 있는 asset이며 이 저장소에 commit하지 않음 | 검증 모델을 배포물에 포함하지 않는 것을 기본으로 합니다. 포함 시 provider license를 별도 확인합니다. |
| COCO class labels | Dataset/annotation source terms apply | 최소/기준: 고정 파일명 models/coco.names 정책: 80개 COCO class label 목록을 생성합니다. snapshot에는 줄 수와 hash를 기록합니다. | models/coco.names: models/coco.names | object detection 표시 이름에 사용하는 models/coco.names label | install script가 표준 COCO class name에서 생성 / 생성되는 로컬 파일이며 dataset 자체에 Apache-2.0을 부여하는 것이 아님 | 번들에 포함하면 dataset/label attribution을 유지하고 프로젝트 원본 콘텐츠처럼 표시하지 않습니다. |
| OpenCV Python | Apache-2.0 upstream project terms | 최소/기준: 미고정. 예제 client가 import 실패 시 설치 안내를 출력합니다. 정책: 예제 client를 실행하는 환경에서 cv2 import version을 snapshot에 기록합니다. | cv2: python3 -c "import cv2; print(cv2.__version__)" | 선택 예제 client-side RTSP/SSE overlay 표시 | Python package manager 또는 시스템 패키지 관리자 / 예제 실행용 optional dependency이며 이 저장소에 vendoring하지 않음 | 예제와 함께 Python package를 bundle하면 OpenCV license와 transitive dependency notice를 포함합니다. |
| Transitive linked libraries from media/runtime packages | Upstream project terms by each linked library | 최소/기준: 직접 고정하지 않음. 상위 media/runtime package와 OS SDK가 결정합니다. 정책: 정확한 파일 경로와 current version은 `./server.sh dependency-snapshot`의 linked library section에 기록합니다. | - | GStreamer, Pango/Cairo, ONNX Runtime, libsodium, system C++ runtime이 끌어오는 GLib, GObject, HarfBuzz, gettext/libintl, libc++/libSystem 같은 실제 linked library | 시스템 패키지 관리자 또는 OS SDK / 직접 소스 의존성은 아니지만 binary bundle에 복사되면 배포 의무가 생길 수 있음 | binary bundle에 dylib/so를 포함하면 snapshot의 linked library 목록을 기준으로 각 upstream license text와 attribution을 추가합니다. |
| CMake, pkg-config, Node.js, Python 3, curl | Upstream project terms | 최소/기준: CMake 3.16+, C++17 compiler, Node.js/Python 3/curl/pkg-config command 필요 정책: CMake 최소 요구 버전은 3.16입니다. 나머지 tool의 실제 버전은 snapshot에 기록합니다. | cmake: cmake --version; pkg-config: pkg-config --version; node: node --version; python3: python3 --version; curl: curl --version | 빌드, 문서/검증 스크립트, HTTP smoke, dependency download helper | 시스템 패키지 관리자 또는 OS 기본 제공 도구 / 외부 build/test tool이며 이 저장소에 vendoring하지 않음 | 배포물에 도구 runtime을 포함하지 않는 것을 기본으로 합니다. 포함하면 각 upstream license를 함께 포함합니다. |
| yt-dlp and Deno | Upstream project terms | 최소/기준: 제품 핵심 실행 필수 조건이 아닙니다. 정책: YouTube import/source 실험 기능을 켠 환경에서만 필요하며 snapshot에 버전을 기록합니다. | yt-dlp: yt-dlp --version; deno: deno --version | 선택 실험 기능인 YouTube import helper path | 시스템 패키지 관리자 / 외부 optional tool이며 이 저장소에 vendoring하지 않음 | 번들에 포함하면 각 tool의 upstream license를 포함하고 YouTube import는 실험 기능으로 유지합니다. |

## Release Checklist

- [ ] binary bundle 안의 동적 library와 plugin 목록을 확인했습니다.
- [ ] model file과 sample media가 bundle에 포함되는지 확인했습니다.
- [ ] `./server.sh dependency-snapshot`으로 실제 설치 버전과 모델 hash를 기록했습니다.
- [ ] 포함되는 third-party license text와 attribution을 bundle에 함께 넣었습니다.
- [ ] FFmpeg/GStreamer plugin build가 GPL component를 포함하는지 확인했습니다.
- [ ] YOLO model asset 재배포/상업 사용 조건을 별도로 확인했습니다.
