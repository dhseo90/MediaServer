# v3.9.0 Evidence 13~14 Implementation Plan

대상 독자는 v3.9.0 Step 26 final evidence를 구현·재감사하는 개발 에이전트입니다.
이 문서는 13~14 개발 동안만 유지하는 실행 계획이며, roadmap 상태의 source-of-truth는
`docs/development-backlog.md`, 테스트 결과의 source-of-truth는
`docs/release-test-records.md`입니다.

**Goal:** 최종 acceptance evidence에서 중복 screenshot, 영상 placeholder, 상수형 cleanup
판정을 제거하고 source commit SHA·실행 명령·첫 실패 지점을 보존한 뒤 독립 bundle을
재실행해 cleanup까지 검증합니다.

**Architecture:** 제품 API/Auth/media/UI 동작은 바꾸지 않습니다. v3.9 UI/longrun/acceptance
runner와 replay verifier만 보강하고, 최종 bundle root는 실행 전에 안전하게 교체합니다.
UI screenshot은 content hash로 중복 파일을 제거하되 case reference는 canonical artifact를
가리키게 합니다. 지원하지 않는 video는 빈 artifact와 명시적 not-captured reason으로 남기며
placeholder 파일을 만들지 않습니다. cleanup은 child summary, port, temp path의 실제 관측값으로
계산합니다.

## Fixed test-category decision

| 테스트 카테고리 | 판정 | 직접 근거 | 실행 승인 상태 |
| --- | --- | --- | --- |
| 안정화 테스트 | 진행 대상 | Evidence 13이 UI/longrun/acceptance runner와 verifier를 변경하고 Evidence 14가 bundle 재실행을 요구 | 최신 `/goal` 승인 |
| 30분 테스트 | 진행 대상 | Evidence 14의 독립 acceptance bundle은 실제 30분 stage를 필수 실행 | 최신 `/goal` 승인 |
| 120분 테스트 | 조건부 진행 | media/runtime 변경은 없으며 안정화/30분 high-risk signal이 있을 때만 실행 | 조건 발생 시에만 |
| UI 풀테스트 | 미진행 | bundle의 UI-108~115 native 자동화는 실행하지만 exact 424개 UI 전수 지시는 아님 | 전수 UI 풀테스트 승인 없음 |

## Task 1 — Evidence 13 final evidence integrity

- [x] roadmap/inventory/release test 항목을 RED 전에 등록합니다.
- [x] RED: placeholder video, duplicate screenshot, 상수 cleanup, provenance 누락 fixture를 contract가 거절하는지 확인합니다.
- [x] UI runner가 실제 PNG를 hash dedupe하고 video placeholder를 만들지 않게 합니다.
- [x] UI/longrun/acceptance cleanup을 filesystem/port/child-summary 관측으로 계산합니다.
- [x] acceptance summary에 source commit SHA, branch, worktree state, 실행 명령 전수, first failure stage/command/context를 기록합니다.
- [x] `verify-v390-final-evidence-integrity` command와 negative contract를 추가합니다.
- [x] 관련 안정화 verifier와 `git diff --check`를 통과해 Evidence 13 커밋 준비를 완료합니다.

## Task 2 — Evidence 14 independent final rerun and cleanup

- [x] Evidence 13 커밋 `b6cac906`의 clean worktree에서 canonical final output을 안전하게 교체했습니다.
- [x] 독립 acceptance bundle을 build → feature gates → 실제 30분 → UI automation/replay → 조건부 120분 판정 → cleanup → report 순서로 실행했습니다.
- [x] 최초 실패 `server-longrun-30`/`integrated-smoke`의 command/context/tail/reproduction과 child cleanup을 보존하고, code-comment header·실패 child summary loading·재시도 first-failure 영구 보존을 같은 범위에서 수정했습니다.
- [x] final integrity verifier, docs/inventory/script gates, `git diff --check`를 실행합니다.
- [x] 삭제 전/후 artifact 수·크기와 보존 사유를 release record에 남깁니다.
- [x] roadmap Step 26과 Evidence 13~14 결과를 직접 evidence로 닫고 Evidence 14 최종 커밋 범위를 준비합니다.

### First execution and recovery boundary

- source: `b6cac906d8b138aee5e0e2e88743ca9aa9a53529`, clean worktree
- first failed stage: `server-longrun-30`; delegated phase/case: `soak-case-loop` / `integrated-smoke`
- actual cause: 통합 smoke의 선행 `verify-code-comments`가 `scripts/internal/ui_fulltest_evidence_policy_v4_lib.mjs` 상단 용도 주석 누락 1건을 검출
- later stages: UI automation, replay, 120-minute decision/run은 stop-on-first-fail에 따라 `not-run`
- cleanup: delegated longrun은 server/8081/8555/predev temp workdir cleanup을 실측 PASS했지만, 상위 bundle이 실패 child summary를 읽지 않아 처음에는 top cleanup을 FAIL로 축약
- recovery: 누락 header를 추가하고 실패 child summary도 읽어 cleanup을 판정하며, 다음 canonical replacement 전에 최초 실패의 진단 artifact tail/hash를 `first-failure.json`/`.md`로 영구 보존
- retry boundary: 복구 변경을 커밋해 clean source SHA를 만든 뒤 acceptance bundle을 build부터 전체 재실행

### Final rerun result

- source: `8fe583d815ce31628cbb8d1f4188b3e6455b396a`, branch `v3.9.0`, clean bundle start
- bundle run: `v390-test-acceptance-20260710160754-54907`, result `PASS`
- 30-minute: predev `pass=118`, `fail=0`, `skip=2`; soak 22회; main/queue idle와 ports-clean PASS
- UI: native Playwright UI-108~UI-115 `8/8`, replay `7/0`; referenced PNG 8개를 물리 4개로 canonicalize, final duplicate 0
- video: capture 미지원은 `not-captured`로 기록, placeholder file 0
- 120-minute: media/session/runtime high-risk signal이 없어 조건부 미실행, PASS 대체 없음
- cleanup: top/longrun/UI child 실측 PASS, predev workdir와 UI registry 제거, 8081/8555/18239/18739 listener 없음
- canonical: 76 files, 1,315,746 bytes; 최초 실패 요약 2개 73,561 bytes 보존
- integrity: `verify-v390-final-evidence-integrity` `7/0`
