# v3.9.0 Review2 26-35 Completion Plan

> 독자: MediaServer 개발·검증 에이전트. Lifecycle: v3.9.0 (22) 잔여이슈 26~35 구현. Source-of-truth: `AGENTS.md`, `docs/development-backlog.md`, `docs/project-feature-test-inventory.md`, `docs/release-test-records.md`.

**Goal:** V390-REVIEW2-26~35를 번호 순서대로 RED→구현→관련 안정화→evidence 기록으로 닫고, 부분 evidence·조건부 미실행·gate 준비를 실제 PASS나 구현 완료로 승격하지 않는다.

**Boundary:** 제품 public API/Event/WebRTC/SSE/WS schema와 RTSP/WebRTC media path는 변경하지 않는다. 30분·120분·Policy v4 exact 424 UI 풀테스트는 별도 사용자 실행 승인 전 자동 실행하지 않는다. 각 번호가 실패하면 같은 번호 안에서 수정·재검증하고 통과 전 다음 번호로 넘어가지 않는다.

## Ordered Tasks

- [x] 26 — acceptance/final integrity에 exact 424, unsupported 0, Policy v4 `uiFulltestPass` 독립 자격을 연결한다.
- [x] 27 — current durable evidence에서 placeholder video와 stale summary binding을 제거하고 historical-invalid 경계를 고정한다.
- [x] 28 — VLM profile save/reload가 공유 full-contract validator를 사용하고 invalid reload profile을 quarantine한다.
- [x] 29 — VLM incident provenance를 server-owned observation/event/candidate record와 대조하고 forged/stale provenance를 no-write로 거부한다.
- [x] 30 — ONVIF source/view paired save rollback을 pre-transaction file bytes/existence/mode 기준으로 복구한다.
- [x] 31 — nested predev phase 결과를 parent ledger에 projection하고 실행 전 synthetic PASS를 제거한다.
- [x] 32 — Re-ID NoOp readiness 실패 시 Enabled/stats/worker/queue/crop 의미를 비활성으로 통일한다.
- [x] 33 — structure readiness에서 실제 include/build graph forbidden edge와 cycle을 검증한다.
- [x] 34 — v3.5~v3.8 exact UI bridge, 30분 count, stale anchor를 current manifest 자동 산출값으로 정렬한다.
- [x] 35 — owner decision, field conditional-not-run, structure gate-ready 상태를 implementation/PASS/complete와 분리한다.
- [x] 현재 스텝 후속 이슈를 재감사하고 없으면 roadmap/evidence를 최종화한 뒤 승인 범위 커밋을 수행한다.
