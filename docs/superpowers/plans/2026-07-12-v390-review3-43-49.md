# v3.9.0 Review3 43~49 Implementation Plan

**Goal:** `V390-REVIEW3-43`부터 `V390-REVIEW3-49`까지 순서대로 닫아 actual Policy v4 evidence, visual 판정, canonical acceptance, current-source integrity, historical 격리, 실제 9-module graph와 구조 실행 결정을 연결합니다.

**Boundary:** 제품 public API, EventRecord, WebRTC/SSE/WS metadata, RTSP/WebRTC media path, Auth/Role/Scope 계약은 변경하지 않습니다. 30분, 조건부 120분, Policy v4 exact 424 UI 풀테스트는 별도 실행 승인 없이 수행하지 않습니다. 각 번호는 테스트 정의 등록 -> RED -> 구현 -> 관련 안정화 검증 -> roadmap/evidence 기록 -> 커밋 순서로 닫고, 실패한 번호가 통과하기 전 다음 번호로 넘어가지 않습니다.

## Fixed test-category decision

| 테스트 카테고리 | 판정 | 직접 근거 | 실행 승인 상태 |
| --- | --- | --- | --- |
| 안정화 테스트 | 진행 대상 | 43~49가 runner, qualifier, final integrity, policy, graph verifier와 문서를 직접 변경 | 최신 요청에서 43~49 개발·이슈 수정 승인 |
| 30분 테스트 | 미진행 | 실행 producer/acceptance 경로를 구현하지만 장시간 실행 자체를 지시하지 않음 | 실행 승인 없음; release blocker 상태는 별도 유지 |
| 120분 테스트 | 조건부 진행 | 45가 AGENTS 7.6.2 조건과 option을 정렬하며 이번 변경은 media/session lifecycle을 직접 변경하지 않음 | trigger 또는 별도 승인 시에만 실행 |
| UI 풀테스트 | 미진행 | actual evidence producer와 visual 판정기를 개발하지만 exact 424 실제 실행 지시는 없음 | 실행 승인 없음; release blocker 상태는 별도 유지 |

## Ordered tasks

- [x] 43 — exact runner가 `media-server.ui-automation-evidence.v4` summary와 case/suite attestation을 직접 생성하고 producer contract가 fixture를 actual evidence로 승격하지 않음을 검증합니다.
- [ ] 44 — actual screenshot/DOM geometry/computed style에서 viewport·theme·overlay·clipping·contrast·focus를 계산하고 visual/cross-cutting PASS의 자기선언을 거부합니다.
- [ ] 45 — canonical command가 30분 longrun -> exact 424 runner -> Policy v4 qualification -> 조건부 120분 -> final integrity를 stop-on-first-fail로 연결합니다.
- [ ] 46 — final integrity가 current HEAD/branch/clean state, canonical command set, child artifact root containment와 historical source 거부를 독립 검증합니다.
- [ ] 47 — stale `0/423/1` coverage 설명을 actual exact manifest 기준으로 교정하고 current consumer가 historical source를 읽지 못하게 source-kind/current binding을 강화합니다.
- [ ] 48 — 9개 declared module 각각을 실제 file/target에 분류하고 include/link graph, forbidden edge, SCC, slice entry/exit를 machine-readable graph evidence로 검증합니다.
- [ ] 49 — 48의 실제 graph와 대형 ingress/UI owner 규모를 근거로 구조 리팩터링 실행 release를 source-of-truth에 확정하고 decision contract로 검증합니다.
